# calculators

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build calculators` to build the library.

## Running unit tests

Run `nx test calculators` to execute the unit tests via [Jest](https://jestjs.io).
